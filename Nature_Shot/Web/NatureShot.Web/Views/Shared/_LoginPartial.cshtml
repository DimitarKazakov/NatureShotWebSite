@using NatureShot.Common
@using NatureShot.Data.Models
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
    @if (this.SignInManager.IsSignedIn(this.User))
    {
<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdownProf" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <span class="line">Profile</span>
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdownProf">
        <a class="dropdown-item" asp-area="" asp-controller="Home" asp-action="Profile" asp-route-username="My" title="Manage"><span class="line">My Profile</span></a>
        <form method="get" class="form-inline" asp-area="" asp-controller="Home" asp-action="OtherProfile">
            <input name="username" class="form-control border-primary" type="text" placeholder="Search..." />
        </form>
    </div>
</li>
                <li class="nav-item">
                    <form id="searchUserProfile" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@(this.Url.Action("Index", "Home", new { area = string.Empty }))">
                        <button type="submit" class="nav-link btn btn-link p-4 rounded text-dark">Logout</button>
                    </form>
                </li>
 }
            else
            {
                if ((string)this.ViewData["Action"] == "Index")
                {
<li class="nav-item">
    <a class="nav-link" asp-area="Identity" asp-page="/Account/Register">Register</a>
</li>
                    <li class="nav-item">
                        <a class="nav-link" asp-area="Identity" asp-page="/Account/Login">Login</a>
                    </li>
 }
                else
                {
<li class="nav-item">
    <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
</li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
                    </li>}
                                }
</ul>
